@model IEnumerable<WardrobeMVC.Models.Recommendation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Accessory.AccessoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Foot.ShoeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Accessory.AccessoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Foot.ShoeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RecommendationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RecommendationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RecommendationID })
        </td>
    </tr>
}

</table>
